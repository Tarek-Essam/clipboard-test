Ticket title
Generate reports with custom agent id

Assumptions
1- all agents has custom id

description
When generating report containing info on how many hours each Agent worked in a given quarter then the report
should use the custom agent id for the facility.

Acceptance Criteria

Given that facility is generating the shifts reports.
Then the id of the agent should be the custom id generated by the facility.

Implementation Details
1- A function getShiftsByFacility is called with the Facility's id, returning all Shifts worked that quarter, including the custom agent id.
2- Join shifts table with Facility_agents and agents table to get agent details.


Time 4h